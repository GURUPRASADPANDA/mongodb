<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Message - WhatsApp Lite</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="chat-container">
        <div class="chat-header">
            <div class="d-flex align-items-center">
                <a href="/chats/<%= chat._id %>" class="back-button" title="Back to chat">
                    <i class="fas fa-arrow-left"></i>
                </a>
                <div class="chat-info ms-3">
                    <h5 class="mb-0">Edit Message</h5>
                </div>
            </div>
        </div>

        <div class="new-chat-container">
            <form action="/chats/<%= chat._id %>/edit" method="POST">
                <div class="form-group mb-3">
                    <label for="from" class="form-label">From</label>
                    <input type="text" 
                           class="form-control" 
                           id="from" 
                           name="from" 
                           value="<%= chat.from %>" 
                           required>
                </div>

                <div class="form-group mb-3">
                    <label for="to" class="form-label">To</label>
                    <input type="text" 
                           class="form-control" 
                           id="to" 
                           name="to" 
                           value="<%= chat.to %>" 
                           required>
                </div>

                <div class="form-group mb-3">
                    <label for="message" class="form-label">Message</label>
                    <textarea class="form-control message-textarea" 
                              id="message" 
                              name="message" 
                              rows="4" 
                              required><%= chat.msg %></textarea>
                </div>

                <div class="d-flex justify-content-between align-items-center">
                    <button type="submit" class="btn btn-send">
                        <i class="fas fa-save me-2"></i>Save Changes
                    </button>
                    <a href="/chats/<%= chat._id %>" class="btn btn-link">
                        <i class="fas fa-times me-2"></i>Cancel
                    </a>
                </div>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
